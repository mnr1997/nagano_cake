<div class="container">
  <div class="row">
    <div class="col-md-10 col-sm-10 col-10 mx-md-auto mx-sm-auto mx-auto rounded px-0">
      <div class="h1">ジャンル編集</div>
      <div>
        <%= form_with model: [:admin, @genre] do |f| %>
          <%= f.label :name %>
          <%= f.text_field :name %>
          <%= f.submit "変更を保存", class: "btn btn-success" %>
        <% end %>
      </div>
    </div>
  </div>
</div>